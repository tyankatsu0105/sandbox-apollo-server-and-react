query Users($page: PaginationInput!, $ids: [ID!]) {
  users(page: $page, ids: $ids) {
    totalCount
    edges {
      cursor
      node {
        id
        name
        birthDay
        createdAt
        favorites {
          books
          movies
          musics
        }
      }
    }
  }
}
